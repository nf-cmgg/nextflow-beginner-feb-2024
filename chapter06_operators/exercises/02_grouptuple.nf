// Use fromPath to create a channel emitting all of 
// the files in the folder ../data/meta/, then use 
// a map to associate the baseName method to each file.
// Finally, group all files that have the same common prefix.