ch1 = Channel.value() // Creates an empty value channel
ch2 = Channel.value('Hello there') // Creates a value channel with a string
ch3 = Channel.value([1, 2, 3, 4, 5]) // Creates a value channel with a list of numbers